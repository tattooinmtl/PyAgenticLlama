{
  "id": "122a5d7c",
  "name": "DynamicTester",
  "description": "",
  "parameters": {},
  "action_type": "python",
  "code": "import pytest\nimport pytest_asyncio\n\nclass DynamicTester:\n    def __init__(self, test_cases: list):\n        self.test_cases = test_cases\n    \n    async def run_tests(self, code: str) -> list:\n        \"\"\"Execute code with various inputs and boundary conditions\"\"\"\n        results = []\n        for case in self.test_cases:\n            try:\n                exec_result = execute_code_safe(code, case)\n                results.append({\n                    'case': case,\n                    'output': exec_result,\n                    'status': 'PASS' if validate_output(code, case, exec_result) else 'FAIL'\n                })\n            except Exception as e:\n                results.append({\n                    'case': case,\n                    'error': str(e),\n                    'status': 'FAIL'\n                })\n        return results",
  "webhook_url": "",
  "enabled": false
}